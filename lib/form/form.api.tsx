import React from "react";
import Form from "./form";

const FormExample: React.FC = () => {
  return (
    <Form
      fields={[]}
      formData={[]}
      onChange={() => {}}
      button={""}
      onSubmit={() => {}}
    ></Form>
  );
};

export default FormExample;
